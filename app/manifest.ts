import type { MetadataRoute } from 'next';
export default function manifest(): MetadataRoute.Manifest { return { name: "Stormin' Shane Weather", short_name: "Stormin Shane", description: "Stormin' Shane live weather, radar, and severe alerts.", start_url: '/', scope: '/', display: 'standalone', background_color: '#030712', theme_color: '#06111f', orientation: 'portrait', categories: ['weather'], icons: [{ src: '/icon.svg', sizes: 'any', type: 'image/svg+xml', purpose: 'maskable' }] }; }
