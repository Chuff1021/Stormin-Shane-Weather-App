import { NextRequest, NextResponse } from 'next/server';
export const dynamic='force-dynamic';
export async function GET(req:NextRequest){const q=req.nextUrl.searchParams.get('q')?.trim(); if(!q)return NextResponse.json({results:[]}); const r=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(q)}&count=8&language=en&format=json`,{next:{revalidate:86400}}); if(!r.ok)return NextResponse.json({error:'geocode failed'},{status:502}); const j=await r.json(); return NextResponse.json({results:(j.results||[]).map((x:any)=>({label:[x.name,x.admin1,x.country_code].filter(Boolean).join(', '),latitude:x.latitude,longitude:x.longitude,name:x.name,admin1:x.admin1,country:x.country}))});}
